# Copy to .env.local (dev) or provide on the server. NEVER commit real values.
PGHOST=your-db-host
PGPORT=5432
PGUSER=your-db-user
PGPASSWORD=your-db-password
PGDATABASE=your-db-name
PGSSL=false
TURNSTILE_SECRET=your-cloudflare-turnstile-secret
